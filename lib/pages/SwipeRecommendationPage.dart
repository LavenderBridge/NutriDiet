import 'package:flutter/material.dart';

class SwipeRecommendationPage extends StatelessWidget {
  const SwipeRecommendationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}